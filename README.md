# Corporate Chats (Корпоративный Мессенджер)

Это продвинутое веб-приложение для корпоративного общения, построенное на современной архитектуре с использованием **PostgreSQL** и **Socket.IO**. Приложение поддерживает обмен сообщениями в реальном времени, передачу файлов различных форматов, видеозвонки (WebRTC), групповые чаты с ролевой моделью и детальное логирование действий.

## 🛠 Технологический стек

* **Язык:** Python 3.13+
* **Web Framework:** Flask 3.0.0
* **Real-time Communication:** Flask-SocketIO (WebSocket)
* **Database:** PostgreSQL (драйвер `psycopg` 3.x)
* **Authentication:** Flask-JWT-Extended (JSON Web Tokens)
* **Configuration:** python-dotenv (управление переменными окружения)
* **Frontend:** HTML5, CSS3, Modern JavaScript (ES6+ модульная структура)

## 🌟 Основной функционал

### 👤 Пользователи и Профиль

* **Авторизация:** Безопасная регистрация и вход с хешированием паролей и JWT-токенами.
* **Расширенный профиль:**
    * Редактирование имени, биографии и пола.
    * **Ввод даты рождения:** Поддержка как выбора через календарь, так и ручного ввода (форматы `DD.MM.YYYY`, `DD.MM`). Автоматический расчет возраста.
* **Управление аватарами:**
    * Загрузка изображений с возможностью обрезки (Cropper.js).
    * **Галерея:** История загруженных аватаров с возможностью быстрого переключения между ними или удаления.
* **Приватность и Блокировка:**
    * Блокировка нежелательных пользователей.
    * **Взаимное скрытие:** Заблокированный пользователь не видит аватар, статус, биографию и время активности блокирующего (и наоборот). В списках и истории чата данные скрываются.
* **Статус:** Отображение "Онлайн" или времени последнего визита с учетом пола (например, "була о 14:00").

### 💬 Чаты и Обмен данными

* **Универсальные сообщения:**
    * Текстовые сообщения с поддержкой ссылок.
    * **Файлы:** Поддержка загрузки и скачивания любых форматов (`PDF`, `DOCX`, `XLSX`, `EXE`, `ZIP`, `MP3` и др.).
    * **Медиа-превью:** Изображения, аудио и видео воспроизводятся прямо в чате.
    * **Подписи к файлам:** Текст и файл отправляются одним сообщением (хранение в формате JSON).
    * **Видео-сообщения:** Запись "кружочков" с камеры с предварительным просмотром перед отправкой.
    * Голосовые сообщения.
* **Реакции и Эмодзи:**
    * Категоризированное меню эмодзи (Смайлы, Жесты, Объекты и т.д., как в Telegram).
    * Всплывающее меню реакций на сообщения (кнопка `➕`).
    * Отображение аватарок пользователей, поставивших реакцию.
* **Управление сообщениями:**
    * Редактирование текста и подписей.
    * Удаление сообщений (для себя или для всех).

### 👥 Управление Группами

* **Ролевая система:** `Owner` (Владелец), `Admin` (Админ), `Member` (Участник).
* **Модерация:**
    * Назначение/снятие админов.
    * Добавление/удаление участников.
    * Редактирование инфо группы (аватар, название).
* **Журнал событий (Audit Logs):**
    * Подробное логирование действий: кто кого добавил, кто изменил название, кто кого удалил.
    * Доступ к логам только для администрации группы.

### 📹 Видеосвязь

* Встроенная поддержка P2P видеозвонков (PeerJS/WebRTC).
* Возможность включения/выключения камеры и микрофона.
* Демонстрация экрана (Screen Sharing).

---

## 🗄 Структура Базы Данных (PostgreSQL)
![ER Диаграмма](./ER.jpg)
Проект использует **PostgreSQL** для надежного хранения данных.

### Основные таблицы

#### 1. `users` (Пользователи)

Хранит учетные данные и профиль.

* `id`: Уникальный идентификатор (хеш).
* `name`: Логин (уникальный).
* `real_name`: Настоящее имя.
* `password_hash`: Хеш пароля.
* `avatars_gallery`: JSON-список ссылок/данных прошлых аватаров.
* `last_active`: Временная метка последней активности.
* *Прочее:* `birth_date`, `gender`, `bio`, `created_at`.

#### 2. `rooms` (Комнаты чатов)

Хранит информацию о беседе.

* `id`: Уникальный ID комнаты.
* `type`: Тип чата (`private` или `group`).
* `deleted_for`: JSON-список пользователей, удаливших чат у себя.
* *Прочее:* `name`, `avatar`, `created_by`.

#### 3. `participants` (Участники)

Связующая таблица «Многие-ко-многим» между пользователями и комнатами.

* `room_id`: Ссылка на комнату.
* `user_id`: Ссылка на пользователя.
* `role`: Роль участника (`owner`, `admin`, `member`).

#### 4. `messages` (Сообщения)

* `room_id`: Куда отправлено.
* `sender_id`: Кем отправлено.
* `type`: Тип контента (`text`, `file`, `voice`, `video`, `system`).
* `content`: Текст сообщения или закодированный файл.
* `deleted_for`: Кто скрыл это сообщение.
* *Временные метки:* `created_at`, `edited_at`.

#### 5. `message_reactions` (Реакции)

* `message_id`: На какое сообщение.
* `user_id`: Кто поставил.
* `reaction`: Текст реакции (эмодзи).

#### 6. `group_logs` (Журнал действий группы)

История административных действий.

* `room_id`: В какой группе.
* `action`: Тип действия (`create`, `update`, `add`, `remove`, `promote`, `demote`).
* `details`: Текстовое описание события.

#### 7. `blocked_users` (Черный список)

* `blocker_id`: Кто заблокировал.
* `blocked_id`: Кого заблокировали.

---

## 🚀 Установка и Запуск

### 1. Предварительные требования
* Python 3.10+
* PostgreSQL (установленный и запущенный сервер)

### 2. Клонирование и окружение

```bash
git clone [https://github.com/AlexMaster168/corporate-chats](https://github.com/AlexMaster168/corporate-chats)
cd corporate-chats

# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate

```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt

```

### 4. Настройка конфигурации (.env)

Создайте файл `.env` в корне проекта и укажите параметры подключения к базе данных и секретные ключи:

```env
# Пример конфигурации .env
DATABASE_URL=postgresql://postgres:password@localhost:5432/chat_db
SECRET_KEY=ваша-супер-секретная-строка
JWT_SECRET_KEY=ваш-секретный-ключ-jwt

```

*Замените `postgres:password` на ваши логин и пароль от PostgreSQL, а `chat_db` на имя базы данных.*

### 5. Инициализация БД и Запуск

Убедитесь, что база данных (например, `chat_db`) создана в PostgreSQL. Таблицы создадутся автоматически при первом запуске.

```bash
python run.py

```

Приложение будет доступно по адресу: `http://127.0.0.1:5000`

*При первом запуске база данных `chat.db` инициализируется автоматически.*

## 📡 API Endpoints & Socket Events

### REST API (`/api/...`)

* `POST /auth/register` — Регистрация.
* `POST /auth/login` — Вход.
* `POST /user/profile` — Обновление био/имени.
* `POST /user/avatar/upload` — Загрузка нового аватара.
* `POST /upload` — Загрузка файла в чат.
* `POST /group/logs` — Получение логов группы (только для админов).

### Основные SocketIO события

* `connect/disconnect` — Управление статусом онлайн.
* `send_message` — Отправка сообщения.
* `new_message` — Получение нового сообщения (broadcast в комнату).
* `edit_message` / `delete_message` — Управление контентом.
* `add_reaction` / `remove_reaction` — Реакции.
* `create_group` / `update_group_settings` — Управление группами.
* `join_video_room` — Подключение к видеозвонку.

---

## 📂 Структура проекта

```text
corporate-chats/
├── app/
│   ├── __init__.py      # Инициализация Flask app
│   ├── api.py           # REST API (Auth, Uploads, Logs)
│   ├── auth.py          # Авторизация (Login/Register)
│   ├── database.py      # Подключение к PostgreSQL и создание таблиц
│   ├── events.py        # Точка входа SocketIO событий
│   ├── extensions.py    # Экземпляры SocketIO и JWT (во избежание циклических импортов)
│   ├── socket_auth.py   # Сокеты: Статус, Профиль
│   ├── socket_chat.py   # Сокеты: Сообщения, Реакции
│   ├── socket_groups.py # Сокеты: Управление группами
│   └── socket_video.py  # Сокеты: Сигналинг видеосвязи
├── static/
│   ├── css/             # Стили
│   └── js/              # Модульный JS (main, api, ui, modules/...)
├── templates/           # HTML шаблоны
├── .env                 # Конфигурация (не комитится в git)
├── requirements.txt     # Зависимости
└── run.py               # Точка входа

```